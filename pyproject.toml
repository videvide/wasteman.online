[project]
name = "wasteman-online"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.15"
dependencies = [
    "django>=6.0.2",
    "pillow>=12.1.1",
    "pytest>=9.0.2",
    "stripe>=14.3.0",
]
